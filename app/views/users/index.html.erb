<h1>Search results for "<%= params[:search] %>"</h1>
<% if @user.any?%>
	<ul>
	<% for user in @user %>
		<li><%= link_to "#{user.fname} #{user.lname} ", user_path(user) %></li>
	<% end %>
	</ul>
<% end %>
